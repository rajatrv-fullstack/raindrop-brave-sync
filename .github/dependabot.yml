version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # Wait a week after a release before proposing it, so a compromised or
    # quickly-yanked action version is not pulled in the same day it appears.
    cooldown:
      default-days: 7
    groups:
      actions:
        patterns: ["*"]
