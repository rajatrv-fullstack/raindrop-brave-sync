name: Semgrep

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "29 6 * * 1"

permissions:
  contents: read

jobs:
  semgrep:
    name: Semgrep (Python, JavaScript, bash, secrets)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
      - name: Install the open-source CLI (no account, no token)
        run: python -m pip install --quiet "semgrep==1.176.1"
      - name: Scan
        run: |
          set +e
          semgrep scan \
            --config p/python --config p/javascript --config p/secrets --config p/default \
            --sarif --output semgrep.sarif --metrics=off
          rc=$?
          echo "semgrep exit code: $rc"
          # 0 = no findings, 1 = findings (still upload); anything else is a real failure
          [ "$rc" -le 1 ] || exit "$rc"
      - name: Upload to code scanning
        uses: github/codeql-action/upload-sarif@6f5948dfacef28e207b48d0905cf90c03365536d # v3
        with:
          sarif_file: semgrep.sarif
          category: semgrep
